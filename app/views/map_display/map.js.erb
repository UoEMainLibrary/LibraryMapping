/**
 * Created by Kristiyan on 05/06/2017.
 */

<%= render 'map_script.js.erb' %>
$('#mappingNavbar').html("<%= escape_javascript(render 'mapping_navbar') %>");
$('.optionsTopBar').html("<%= escape_javascript(render 'subheader') %>");
$('#compass').html("<%= escape_javascript(render 'compass') %>");

<% if @searching_element && !@elementnames.any? %>
$('.<%=@element_name%>').notify('We could not locate this item on this floor. Please ask someone at the Helpdesk.');
<% end %>